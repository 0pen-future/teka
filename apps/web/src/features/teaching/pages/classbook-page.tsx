/** Quản lý lớp học — filled out in phases 3–4 (sessions, nhận xét, giáo án). */
export function ClassbookPage() {
  return (
    <header>
      <h1 className="font-display text-[26px] font-extrabold text-ink-900">Quản lý lớp học</h1>
      <p className="mt-1 text-[13.5px] text-ink-500">
        Buổi học, điểm số, nhận xét và giáo án của từng lớp.
      </p>
    </header>
  );
}
