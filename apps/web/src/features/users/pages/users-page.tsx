import { PageHeader } from "@/components/shared/page-header";

/** Placeholder — the users table lands with the frontend features phase. */
export function UsersPage() {
  return (
    <div className="space-y-6">
      <PageHeader title="Users" description="User management arrives in a later phase." />
    </div>
  );
}
