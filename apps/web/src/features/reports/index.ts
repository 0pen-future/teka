// Public surface of the reports feature. The app router imports ONLY from
// here to mount the send-reports period list; nothing else in the app needs
// to depend on this feature.
export { reportsRoutes } from "./routes";
